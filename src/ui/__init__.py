"""
Tab modules for the Streamlit dashboard.

Each tab lives in its own file and exposes a single ``render()`` function, so
app.py holds layout and wiring only. This also lets two people build different
tabs at the same time without ever editing the same file.
"""
