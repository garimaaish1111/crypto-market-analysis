# Pinned to the exact versions this project was developed and tested against.
#
# These were previously open ranges (`pandas>=2.0`). That is not reproducible:
# the same file resolved to a different pandas major version depending on when
# it was installed, and the code was then running on a version nobody had
# tested it against. Everything below is now pinned, and CI verifies the set
# installs and passes on Python 3.12 and 3.13.
#
# Regenerate with: pip freeze > requirements.lock.txt

# Core data / analysis
pandas==3.0.5
numpy==2.5.2
scipy==1.18.0
statsmodels==0.14.6

# Machine learning — direction classifier and volatility model
scikit-learn==1.9.0
arch==8.0.0

# Data sources
requests==2.34.2
yfinance==1.6.0

# Dashboard & charts
streamlit==1.61.1
plotly==6.9.0
