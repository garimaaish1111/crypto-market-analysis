# Pinned to the versions this project was developed and tested against.
# Unpinned ranges (`pandas>=2.0`) silently resolved to pandas 3.x, a major
# version the code had never been run on — reproducible builds need exact pins.
# Regenerate with: pip freeze > requirements.lock.txt

# Core data / analysis
pandas==3.0.5
numpy==2.5.2
scipy==1.18.0
statsmodels==0.14.6

# Machine learning — direction classifier and volatility model
scikit-learn==1.9.0
arch==8.0.0

# Data sources
requests==2.34.2
yfinance==1.6.0

# Dashboard & charts
streamlit==1.61.1
plotly==6.9.0
